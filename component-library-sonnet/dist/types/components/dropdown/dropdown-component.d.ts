import { EventEmitter } from "../../stencil-public-runtime";
import { ItemConfiguration } from "../../core/components/item/domain/ItemConfiguration";
import { Item } from "../../core/components/item/domain/Item";
export declare class DropdownComponent {
    static readonly TAG_NAME: string;
    private element;
    opened: boolean;
    placeholderText: string;
    labelText: string;
    itemConfiguration: ItemConfiguration;
    required: boolean;
    name: string;
    selectedItem: Item;
    currentItem: Item;
    dropdownSelectionChange: EventEmitter;
    private dropdownButtonElement;
    private textValueElement;
    private keyboardEventProcessor;
    private items;
    private hiddenEl;
    constructor();
    open(): Promise<void>;
    close(): Promise<void>;
    toggle(): Promise<void>;
    isOpen(): Promise<boolean>;
    setValue(value: any): Promise<void>;
    getValue(): Promise<string>;
    setIndex(index: any): Promise<void>;
    getIndex(): Promise<number>;
    handleModalOpenAndClose(opened: boolean): void;
    handleSelectedItemChange(): void;
    componentWillLoad(): void;
    componentDidUpdate(): void;
    componentDidLoad(): void;
    handleClick(item: Item): void;
    render(): any;
    private initItemConfiguration;
    private processArrowKeys;
    private getNextAvailableItem;
    private processKeyboardEvent;
}
