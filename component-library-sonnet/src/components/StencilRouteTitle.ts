/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import { Components, JSX } from 'component-library';


interface StencilRouteTitleProps {
  
  /**  */
  titleSuffix?: Components.StencilRouteTitle["titleSuffix"]
  
  /**  */
  pageTitle?: Components.StencilRouteTitle["pageTitle"]
}

interface StencilRouteTitleEvents {
  
}

interface StencilRouteTitleSlots {
  default: any
}
  
/* generated by Svelte v3.35.0 */
import {
	SvelteComponent,
	binding_callbacks,
	create_slot,
	detach,
	element,
	init,
	insert,
	safe_not_equal,
	set_custom_element_data,
	transition_in,
	transition_out,
	update_slot
} from "svelte/internal";

import { createEventDispatcher, onMount } from "svelte";

function create_fragment(ctx) {
	let stencil_route_title;
	let current;
	const default_slot_template = /*#slots*/ ctx[5].default;
	const default_slot = create_slot(default_slot_template, ctx, /*$$scope*/ ctx[4], null);

	return {
		c() {
			stencil_route_title = element("stencil-route-title");
			if (default_slot) default_slot.c();
			set_custom_element_data(stencil_route_title, "title-suffix", /*titleSuffix*/ ctx[0]);
			set_custom_element_data(stencil_route_title, "page-title", /*pageTitle*/ ctx[1]);
		},
		m(target, anchor) {
			insert(target, stencil_route_title, anchor);

			if (default_slot) {
				default_slot.m(stencil_route_title, null);
			}

			/*stencil_route_title_binding*/ ctx[6](stencil_route_title);
			current = true;
		},
		p(ctx, [dirty]) {
			if (default_slot) {
				if (default_slot.p && dirty & /*$$scope*/ 16) {
					update_slot(default_slot, default_slot_template, ctx, /*$$scope*/ ctx[4], dirty, null, null);
				}
			}

			if (!current || dirty & /*titleSuffix*/ 1) {
				set_custom_element_data(stencil_route_title, "title-suffix", /*titleSuffix*/ ctx[0]);
			}

			if (!current || dirty & /*pageTitle*/ 2) {
				set_custom_element_data(stencil_route_title, "page-title", /*pageTitle*/ ctx[1]);
			}
		},
		i(local) {
			if (current) return;
			transition_in(default_slot, local);
			current = true;
		},
		o(local) {
			transition_out(default_slot, local);
			current = false;
		},
		d(detaching) {
			if (detaching) detach(stencil_route_title);
			if (default_slot) default_slot.d(detaching);
			/*stencil_route_title_binding*/ ctx[6](null);
		}
	};
}

function instance($$self, $$props, $$invalidate) {
	let { $$slots: slots = {}, $$scope } = $$props;
	let __ref;
	let __mounted = false;
	const dispatch = createEventDispatcher();
	let { titleSuffix = undefined } = $$props;
	let { pageTitle = undefined } = $$props;
	const getWebComponent = () => __ref;

	onMount(() => {
		__mounted = true;
	});

	const setProp = (prop, value) => {
		if (__ref) $$invalidate(2, __ref[prop] = value, __ref);
	};

	const onEvent = e => {
		e.stopPropagation();
		dispatch(e.type, e.detail);
	};

	function stencil_route_title_binding($$value) {
		binding_callbacks[$$value ? "unshift" : "push"](() => {
			__ref = $$value;
			$$invalidate(2, __ref);
		});
	}

	$$self.$$set = $$props => {
		if ("titleSuffix" in $$props) $$invalidate(0, titleSuffix = $$props.titleSuffix);
		if ("pageTitle" in $$props) $$invalidate(1, pageTitle = $$props.pageTitle);
		if ("$$scope" in $$props) $$invalidate(4, $$scope = $$props.$$scope);
	};

	return [
		titleSuffix,
		pageTitle,
		__ref,
		getWebComponent,
		$$scope,
		slots,
		stencil_route_title_binding
	];
}

class StencilRouteTitle extends SvelteComponent {
  $$prop_def: StencilRouteTitleProps;
  $$events_def: StencilRouteTitleEvents;
  $$slot_def: StencilRouteTitleSlots;

  $on<K extends keyof StencilRouteTitleEvents>(type: K, callback: (e: StencilRouteTitleEvents[K]) => any): () => void {
	  return super.$on(type, callback);
	}

  $set($$props: Partial<StencilRouteTitleProps>): void {
	  super.$set($$props);
	}

	constructor(options) {
		super();

		init(this, options, instance, create_fragment, safe_not_equal, {
			titleSuffix: 0,
			pageTitle: 1,
			getWebComponent: 3
		});
	}

	get getWebComponent(): HTMLStencilRouteTitleElement | undefined {
		return this.$$.ctx[3];
	}
}

export default StencilRouteTitle;